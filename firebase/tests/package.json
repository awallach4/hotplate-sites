{
  "name": "firebase-tests",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.4",
    "firebase": "^9.9.1",
    "mocha": "^10.0.0",
    "prettier": "^2.7.1"
  },
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"mocha --exit firestorerules.spec.js --timeout 10000\""
  }
}
